using SymphonySolutions.UI.Helpers;

namespace SymphonySolutions.UI.PageObjects
{
    public sealed class MainPage : BasePage
    {
        public MainPage(IDriverWraper driverWraper) : base (driverWraper) { }
    }
}
