using OpenQA.Selenium;
using SymphonySolutions.UI.Helpers;

namespace SymphonySolutions.UI.PageObjects
{
    public sealed class VacancyPage : BasePage
    {
        public VacancyPage(IDriverWraper driverWraper) : base(driverWraper)
        {
        }
    }
}
